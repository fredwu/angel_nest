hr
.micro_post_user
  = render 'users/avatar', :user => micro_post.user, :size => 50
.micro_post_container
  .micro_post_meta
    = time_ago_by(micro_post.created_at)
    '
    = link_to micro_post.user.name, resource_path(micro_post.user.username)

  .micro_post_content= micro_post.content
